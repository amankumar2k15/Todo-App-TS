import TodoContainer from "@/component/TodoContainer";
import React from "react";

const Home = () => {
  return (
    <div className="bg-[#0D0714] min-h-screen flex items-center justify-center">
      <TodoContainer />
    </div>
  );
};

export default Home;
